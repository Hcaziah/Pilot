using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CorsairGimbal : MonoBehaviour {

	void Start () {
	}
	
	// Update is called once per frame
	void Update () {	
		/*double thisPositionY = transform.position.y;
		double thisPositionX = transform.position.x;
		double thisPositionZ = transform.position.z;
		double thisRotation = transform.rotation.x;
		thisPositionY = 4.5;
		thisPositionX = 
		thisPositionZ = 
		thisRotation = 0;*/
	}
}
